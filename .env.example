# JIRA Configuration
JIRA_HOST=your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token

# Server Configuration
PORT=3001
NODE_ENV=development

# Database (we'll use JSON file storage for simplicity)
DATA_DIR=./data
